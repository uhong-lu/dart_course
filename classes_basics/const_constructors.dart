
class Complex {
  //Complex(this.re, this.im);
  const Complex(this.re, this.im);
  final double re;
  final double im;
}

void main(){
  // const complex = Complex(1, 2); //the constructor being called isn't a const constructor //condt variable must be initialized with a constant value
  //final complex = Complex(1, 2);
  // const x = 10;
  // const list = [
  //   Complex(1, 2),
  //   Complex(3, 4),
  // ];
}







// /**
//  * 主題：const 構造函式 (Const Constructors)
//  * 核心觀念：只有當類別所有成員都是 final 時，才能定義 const 建構子，藉此產生「規範化 (Canonicalization)」物件。
//  */

// // [class]：定義自定義型別 Complex (複數)。
// class Complex {
//   // ---------------------------------------------------------
//   // 1. Const 構造函式 (Const Constructor)
//   // ---------------------------------------------------------
  
//   // [const]：關鍵字。告訴編譯器：如果傳入的引數是常數，則在「編譯時」就直接產生物件實體。
//   // [Complex]：構造函式名稱。
//   // [(this.re, this.im)]：參數 (Parameters)。
//   // [this shorthand syntax]：自動將參數賦值給同名的成員變數 re 與 im。
//   const Complex(this.re, this.im);

//   // ---------------------------------------------------------
//   // 2. 不可變成員變數 (Immutable Member Variables)
//   // ---------------------------------------------------------
  
//   // [final]：強制要求。若要定義 const 建構子，所有屬性「必須」是 final。
//   // [double]：型別物件。
//   // [re], [im]：成員變數 (Properties)，分別代表複數的實部與虛部。
//   final double re;
//   final double im;
// }

// // [void main()]：進入點函式。
// void main() {
//   // ---------------------------------------------------------
//   // 3. 常數物件實例化 (Constant Instantiation)
//   // ---------------------------------------------------------
  
//   // [const complex]：宣告一個編譯期常數變數。
//   // [Complex(1, 2)]：呼叫 const 構造函式。
//   // [1, 2]：常數引數 (Constant Arguments)。
//   // 💡 記憶體特點：如果在程式其他地方又寫了 const Complex(1, 2)，
//   // 它們會指向 Heap 中完全「同一個」地址，這稱為 Canonicalization。
//   const complex = Complex(1, 2); 

//   // [const x]：基礎型別的編譯期常數。
//   const x = 10;

//   // [const list]：建立一個不可變的清單物件。
//   // [Complex(1, 2)]：這裡雖然沒寫 const，但因為在 const list 內部，
//   // 系統會自動將其視為 const Complex(1, 2)。
//   const list = [
//     Complex(1, 2),
//     Complex(3, 4), // 列表內的第二個物件實體。
//   ];
// }

// /**
//  * 🔬 深度技術解析：
//  * * 1. [為什麼要用 const？]：
//  * 想像你有一個 UI 按鈕，它的顏色、大小永遠不變。使用 const 建立它，
//  * 每次螢幕重繪 (Re-build) 時，Flutter 都不需要重新建立物件，直接拿舊的用。
//  * * 2. [final vs const]：
//  * - final：我保證物件建立後「值」不會變。
//  * - const：我保證這個物件在「寫程式碼時」就已經定死在記憶體中了。
//  * * 3. [約束條件]：
//  * 如果你嘗試在一個有非 final 變數的類別加上 const 建構子，編譯器會噴錯，
//  * 因為「常數」絕對不能包含「可能會變」的東西。
//  */