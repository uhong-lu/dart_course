class Person {
  Person({
    required this.name,
    required this.age,
    required this.height
    });
  
  final String name;
  final int age;
  final double height;

  void printDescription() {
    print("My name is $name. I'm $age years old, I'm $height meters tall.");
  }
}

void main() {
  //MAP  final person = <String dynamic>{'name': 'Alice', 'age': 26, 'height': 1.74};
  //Class final p1 = Person(name: 'Alice',age: 26,height: 1.74);
  final p2 = Person(name: 'Lucy',age: 16,height: 1.84);
  //p1.printDescription();
  p2.printDescription();
}






// /**
//  * 主題：類別的型別安全性 (Type Safety with Classes)
//  * 核心：對比 Map 的靈活但危險，以及類別的嚴謹與安全。
//  */

// // [class]：關鍵字，定義一個自定義的「型別 (Type)」。
// // [Person]：類別名稱。它定義了物件應該擁有的結構與行為。
// class Person {
//   // ---------------------------------------------------------
//   // 1. 構造函式與成員初始化 (Constructor & Initialization)
//   // ---------------------------------------------------------
  
//   // [Person({ ... })]：構造函式。
//   // [{ }]：具名參數 (Named parameters)，讓呼叫端必須明確指定參數名稱。
//   // [required]：修飾詞，確保該參數絕對不能遺漏，否則編譯失敗。
//   // [this.name]：Dart 的「this 簡寫語法 (this shorthand syntax)」。
//   //    - 它在背景執行了「定義參數」並「將傳入的引數 (Argument) 賦值給成員變數」。
//   Person({
//     required this.name,
//     required this.age,
//     required this.height,
//   });
  
//   // ---------------------------------------------------------
//   // 2. 不可變成員變數 (Immutable Member Variables / Properties)
//   // ---------------------------------------------------------
  
//   // [final]：確保屬性在初始化後不可修改，提升安全性。
//   // [String], [int], [double]：明確的型別標註。
//   // 💡 與 Map 不同：類別在編譯時就確定了屬性的型別，Map 則常使用 dynamic。
//   final String name;
//   final int age;
//   final double height;

//   // ---------------------------------------------------------
//   // 3. 實例方法 (Instance Method)
//   // ---------------------------------------------------------

//   // [void]：不回傳資料。 [printDescription]：定義在物件上的行為。
//   // 💡 Map 無法直接擁有方法，只能儲存資料。
//   void printDescription() {
//     // [$name]：字串插值，直接存取物件內部的強型別屬性。
//     print("My name is $name. I'm $age years old, I'm $height meters tall.");
//   }
// }

// // [void main()]：進入點函式。
// void main() {
//   // ---------------------------------------------------------
//   // 4. Map vs Class 深度對比 (Comparison)
//   // ---------------------------------------------------------

//   // [方式 A: Map] (通常用於暫時、非結構化的資料)
//   // ❌ 缺點 1：Key 是字串。如果你打錯成 'nane'，編譯器不會警告，執行時會噴錯。
//   // ❌ 缺點 2：型別是 dynamic。存取時需要手動轉型 (as int)，非常麻煩且不安全。
//   // final personMap = <String, dynamic>{'name': 'Alice', 'age': 26, 'height': 1.74};

//   // [方式 B: Class] (用於正式、具備邏輯的資料結構)
//   // ✅ 優點 1：拼字檢查。如果你把 name: 打成 nane:，IDE 會立刻顯示紅線報錯。
//   // ✅ 優點 2：自動補全。打出 p1. 之後，IDE 會告訴你它有 age 和 height。
  
//   // [p1]：物件實例 (Object Instance)。
//   // [name: 'Alice']：具名引數 (Named argument)。
//   // [26]：引數 (Argument)，必須符合 int 型別，不能傳入字串。
//   final p1 = Person(name: 'Alice', age: 26, height: 1.74);

//   // [p2]：另一個獨立的物件。
//   final p2 = Person(name: 'Lucy', age: 16, height: 1.84);

//   // ---------------------------------------------------------
//   // 5. 呼叫方法 (Method Invocations)
//   // ---------------------------------------------------------
  
//   // [p1.printDescription()]：執行 p1 物件的特定行為。
//   p1.printDescription();
//   // [p2.printDescription()]：執行 p2 物件的特定行為。
//   p2.printDescription();
// }

// /**
//  * 🔬 深度總結：為什麼類別 (Class) 代表「安全」？
//  * 1. [Early Error Detection]：類別讓你在寫程式時就知道錯在哪，而不是在 App 跑起來崩潰後才知道。
//  * 2. [Compiler Support]：編譯器知道 Person 類別的「形狀」，能提供極佳的開發輔助。
//  * 3. [Encapsulation]：類別將資料與操作資料的方法打包在一起，Map 僅能單純裝載數據。
//  */